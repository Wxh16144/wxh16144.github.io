<% if (page.total > 1) { %>
<nav class="pagination">
  <% if (page.prev) { %><a href="<%= url_for(page.prev_link) %>">&larr; newer</a><% } %>
  <% if (page.next) { %><a href="<%= url_for(page.next_link) %>">older &rarr;</a><% } %>
</nav>
<% } %>
